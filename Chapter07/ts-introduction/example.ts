let title: string = "RSS Aggregator";
//title = 1;

//let arr: string[];
let arr: Array<string>;

let obj: { foo: string, bar: number };

function request( options: { uri: string, method: string } ){

}